plugins {
    id "com.github.node-gradle.node" version "5.0.0"
}

repositories {
    mavenCentral()
}

node {
    download = true
    version = "22.12.0"
}

tasks.register('npmBuild', NpmTask) {
    args = [ 'run', 'build' ]
}